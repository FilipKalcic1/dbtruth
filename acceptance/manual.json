[
  { "task": "T0.1", "id": "A2", "part": "acceptance", "item": "npm run acceptance -- --task T0.1 prints 100. A command check would run the acceptance script inside itself, so the evidence is the score line of a real run (NOTES.md, 0.2.0).", "evidence": "PROGRESS.md, T0.1 iteration 4: npm run acceptance -- --task T0.1 printed T0.1: 100/100 and exited 0, every check and item passing. The run before it, with this evidence empty, printed T0.1: 87/100 with only this item failing." },
  { "task": "T0.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T0.1 iteration 2: five sabotages of scripts/acceptance.mjs (gate cap removed, no kill at the timeout, empty parts earn their weight, record ignored, exit check inverted), each failing a named test with its message, each restored byte for byte. The hang test was strengthened after it stayed green under the second." },
  { "task": "T0.2", "id": "A1", "part": "acceptance", "item": "All eight matrix cells are green on main: the link to the CI run on main.", "evidence": "https://github.com/FilipKalcic1/dbtruth/actions/runs/35908505808: all eight cells green on main (Postgres 12, 14, 16, 18 by Node 20, 22) after PR #3 merged, 82 tests passing and 2 live tests skipped in each." },
  { "task": "T0.2", "id": "A2", "part": "acceptance", "item": "The sabotage branch is red: the link to its CI run.", "evidence": "https://github.com/FilipKalcic1/dbtruth/actions/runs/35907874085: branch ci-sabotage (an effortFor assertion changed from low to high in test/config.test.ts) red in all eight cells, each failing on not ok 16 - effort follows schema size... expected high, actual low. The branch was then deleted, locally and on GitHub." },
  { "task": "T0.2", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T0.2 iteration 1: Postgres 12 dropped from the matrix and scale.sql unmounted from docker-compose.yml; each failed its test in test/ci.test.ts; both files restored. The CI-side sabotage branch is A2." },
  { "task": "T1.2", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T1.2 iteration 2: three sabotages of src/cli.ts (version option removed, package.json read from the working directory, commander's default -V), each failing the --version test in test/cli.test.ts with its message; the second also failed the package smoke test. src/cli.ts restored byte for byte each time." },
  { "task": "T1.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T1.1 iteration 2: ten sabotages, five of findDotEnv in src/safety.ts (start directory only, walk outside a repository, .git file ignored, root .env wins, unreadable .env ends the search) and five of run() in src/cli.ts (.env beats the environment, reading-settings line inverted, --env-file existence check removed, --env-file merged with the .env found, values printed on the reading-settings line), each failing a named test with its message. PROGRESS.md, T1.1 iteration 4: six sabotages of what changed since, the option renamed --dotenv among it: four of run() in src/cli.ts (--dotenv existence check removed, --dotenv merged with the .env found, the no-database-URL error printing the file's values, which the A6 canary test caught, the could-not-read line never printed) and two of findDotEnv in src/safety.ts (an unreadable .env's error dropped from searched, a .env in a middle directory passed over), each failing a named test with its message. Both files restored byte for byte each time." },
  { "task": "T1.3", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T1.3 iteration 2: six sabotages, two of connect() in src/safety.ts (the driver's message thrown again in place of the sentence per cause, the client left open when the session setup is refused), three of src/doctor.ts (a failed read-only proof no longer leaving the setup not ready, the Node check refusing Node 20, check 7 counting every relation as readable) and one of src/cli.ts (the doctor command exiting 0 whatever the checks found), each failing a named test with its message. The proof sabotage first left every test green, because the only server that accepted the write also said it was Postgres 9; the test was strengthened with a Postgres 16 server whose proof alone fails, and the repeated sabotage failed it. Each file restored byte for byte." },
  { "task": "T1.5", "id": "A2", "part": "acceptance", "item": "A person who has never seen the tool follows the README's quick start on the fixture in under five minutes. The lead accepts a fresh agent standing in for that person, said as such: one that knows nothing of this repository and is given only the quick start and the packed package, installed into an empty project. Evidence: who followed it, the time, and every point of friction, as recorded in PROGRESS.md.", "evidence": "Two walkthroughs by an agent standing in for a newcomer, not a person: a fresh agent that knew nothing of the repository, given only the README and the packed package installed into an empty git repository, against the fixture, with no API key. First (07:35): a passing doctor after 33 s (about 3 min for a person reading), from the root and from packages/api; five points of friction, all fixed (PROGRESS.md, T1.5 iteration 4). Second, on the fixed README (08:20): a passing doctor after 31 s (4-5 min estimated for a person reading at a normal pace), every line and message as the README says; two small gaps, fixed. Without a key both stopped at the full run, as the README says they would. A person should repeat it before release." },
  { "task": "T1.5", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T1.5 iteration 2: four sabotages, three of README.md (the row for an unreadable .env deleted, the quick start's .env given back its commented model line, the trimmed-input row cut back to its first note) and one of connectFailure in src/safety.ts (the fallback without its code), each failing a named test in test/readme.test.ts or test/doctor.test.ts with its message. The trimmed-input sabotage first left every test green, because the notes fitToContext returns as reduced: were not read; the test was strengthened to read them as a sixth way, and the repeated sabotage failed it. PROGRESS.md, T1.5 iteration 3: three sabotages of what changed since: two of doctor's line for a missing key in src/doctor.ts (marked ok again, naming check and mcp again), each failing the tests in test/doctor.test.ts that pin the line, the command's key check among them, and one of explainApiFailure in src/model.ts (the SDK's words given back to a missing key), failing the no-key test in test/integration.test.ts with its message. Each file restored byte for byte each time." },
  { "task": "T2.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T2.1 iteration 2: eight sabotages, six of src/extract.ts (estimateRows returning -1 before the pilot, listRelations no longer handing on a partitioned table's leaves, isUnknown without the Postgres 12 and 13 form of 0 over pages on disk, pages read from relpages alone without pg_relation_size, the pilot without REPEATABLE, a partitioned table's own reltuples trusted before its leaves), one of tableFile in src/write.ts (the pilot condition inverted) and one of DESCRIBED_RELATIONS in src/safety.ts (the temporary schemas listed again), each failing a named test in test/sampling.test.ts, test/extract.test.ts or test/write.test.ts with its message. The unseeded pilot was caught by the determinism test on four runs out of four. No sabotage left every test green. Each file restored byte for byte each time." },
  { "task": "T3.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T3.1 iteration 2: ten sabotages, six of src/snapshot.ts (canonical no longer sorting keys, toSnapshot marking no relation as not examined, parseSnapshot returning before holding measuredWith to the flags' ranges, readSnapshot without its size check, toSnapshot also writing Verified.tables, the fingerprint over the relation names alone), one of src/cli.ts (the snapshot not handed to persist), one of previousOutputs in src/write.ts (snapshot.json no longer cleared), one of claimsSchema in src/schemas.ts (the first of two copies with one basis kept again) and one of deadTableQuery in src/verify.ts (the age without ceil), each failing a named test in test/snapshot.test.ts, test/integration.test.ts, test/write.test.ts, test/schemas.test.ts or test/verify.test.ts with its message. The previousOutputs sabotage first failed its test with a message assert generated, quoting another test's source; the assertion was given a message saying what it checks, and the repeated sabotage failed with it. No sabotage left every test green. Each file restored byte for byte each time." },
  { "task": "T3.2", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T3.2 iteration 2: twelve sabotages of the task's core, nine of src/check.ts (remeasure measuring with this run's settings instead of the snapshot's measuredWith, classify with regression and improved swapped, missingName blind to a dropped column, diff blind to an added relation, remeasure profiling every relation, any move of the hit rate called drift, stale not failing under regression, not measured failing under change, the report line without its hit rates) and three of src/cli.ts (runCheck's exit code inverted, runCheck connecting before it reads the snapshot, the check command ignoring --fail-on), each failing a named test in test/check.test.ts or test/remeasure.test.ts with its message. The same iteration records nine sabotages of its own fixes. No sabotage left every test green. Each file restored byte for byte each time." },
  { "task": "T6.1", "id": "A2", "part": "acceptance", "item": "Nothing in the README's Team tier section promises a feature that is not built or planned in BUILD_PLAN.md: every sentence, each checked against the plan and the code. The part a script can judge, the commands the section names and whether each is called coming, is the test \"the Team tier section names every command, and only those not built yet as coming\" in test/readme.test.ts. The price and the waitlist form are HUMAN and not part of it.", "evidence": "PROGRESS.md, T6.1 iteration 1, and iteration 2 for the third sentence, which now says \"free forever\" as the plan does: the section's nine sentences, each against BUILD_PLAN.md and src/, and the commands test passing. (1) The Team tier is the Action on private repositories: section 2, the tier table. (2) The Action is coming; on a pull request it will run dbtruth check against the database the workflow gives it, write what moved into one comment updated in place, and by default fail the job on a regression or a stale item: T4.1 (P1, not built; the database-url and fail-on inputs, the comment script, A2), T3.2's table (stale fails under regression), T3.3. (3) Free forever: the CLI with dbtruth, doctor and check, the commands src/cli.ts defines, and init and mcp called coming (T1.4, T5.1, the Commands list); the skill called coming, which tells an agent when to read the context and measure a join (T5.2, Appendix D); the Action on public repositories (section 2, T6.2). (4) No database content passes through a server of ours: section 2's goal, T6.1, R3, R9; a hosted service is out of scope. (5) Own model access: src/cli.ts hands ANTHROPIC_API_KEY to the model client of src/model.ts; check needs no model: src/check.ts never imports model.ts, pinned by test/structure.test.ts. (6) The Action runs check in the user's CI job: T4.1, a composite action. (7) On a private repository a license check sends the key and the repository id and nothing else, and an outage never fails the job: T6.2 (P2), its fail-open rule and A2. (8) PRICE_TBD per team per month: section 2, a price HUMAN decides. (9) The waitlist link: T6.1 HUMAN, T6.3 step 3. Nothing else is named: no dashboard, hosted service, SLA, support tier or UI. PROGRESS.md, T4.1 iteration 1: the section moved after the README's CI section, and the two sentences that changed, (2) and (6), now in the present tense as the Action ships with 0.3.0, re-checked against the plan: (2) the Action (see CI) runs dbtruth check on a pull request against the database the workflow gives it, writes what moved into one comment updated in place, and by default fails the job on a regression or a stale item: T4.1 (the database-url and fail-on inputs, the comment script, A2), T3.2's table, T3.3's comment; (6) the Action runs check in the user's own CI job: T4.1, a composite action. (7), the license check, stays in the future tense: T6.2 is not built. The other sentences are unchanged, and the commands test passes." },
  { "task": "T6.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T6.1 iteration 2: five sabotages of the Team tier section in README.md (the section taken out whole, the price sentence deleted, the waitlist link unlinked without its placeholder, \"the coming\" dropped before `init` and `mcp`, `check` dropped from the sentence on what stays free forever), each failing a named test in test/readme.test.ts with its message. The last first left every test green, because the commands test gathered the commands over the whole section, which names `check` three times more; the test was strengthened to hold the sentence on what stays free forever to every command, and the repeated sabotage failed it. README.md restored byte for byte each time." },
  { "task": "T1.4", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T1.4 iteration 2: five sabotages of runInit in src/cli.ts (the existing-file check dropped and the .env opened with w, the .env written in the working directory rather than at the repository root, the .gitignore warning's condition inverted, --no-index dropped from git check-ignore, DATABASE_URL written uncommented), each failing a named test in test/init.test.ts with its message. The first failed its test with a dump of the file's bytes under a message assert generated; the comparison was given a message saying what it checks, and the repeated sabotage failed with it. The --no-index sabotage first left every test green, because no test had a .env git tracks; a test of that case was added, and the repeated sabotage failed it. The same iteration records four sabotages of its own fixes. src/cli.ts restored byte for byte each time." },
  { "task": "T2.3", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T2.3 iteration 2: nine sabotages of the task's core, five of src/verify.ts (the condition's filter and its parameter dropped, the value written into the statement as a literal in place of $1, the kept query without its closing note, the check that the condition's column exists removed, the categorical gate removed), one each of src/schemas.ts (relationshipId without the [column=value] suffix), src/safety.ts (querySampled's plain-form retry without the parameters), src/write.ts (the edge without its condition) and src/check.ts (claimNames without the condition's column), each failing a named test in test/joins.test.ts, test/verify.test.ts, test/schemas.test.ts or test/check.test.ts with its message. The same iteration records three sabotages of its own fixes. No sabotage left every test green. Each file restored byte for byte each time." },
  { "task": "T2.4", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T2.4 iteration 2: eight sabotages of the task's core, five of src/verify.ts (the ends' names swapped, the integer gate weakened to either column, the key's highest value returned beside the counts, the ends counted in the hashed form where the column does not lead the key, the returned row checked for orphansAbove in place of orphans_above so the counts are never read), two of src/write.ts (orphanShape returning at once, the orphans inside the range computed without those below) and one of src/safety.ts (isIntegerType by substring), each failing a named test in test/joins.test.ts, test/verify.test.ts, test/write.test.ts or test/safety.test.ts with its message. The same iteration records two sabotages of its own fixes. No sabotage left every test green. Each file restored byte for byte each time." },
  { "task": "T2.2", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T2.2 iteration 2: nineteen sabotages of the task's core, fourteen of src/verify.ts (the density test inverted, the density test dropped, the containment of the ranges inverted, the target kept among the keys, the from-column's own key kept, the declared foreign key, the basis and the join.confirmed bound each dropped from worthWeighing, the weighing returning the column's ends, the probe returning the key's highest value as its span, a weighing that did not run returning alsoFits 0, a branch weighed over every sampled row, the keys probed again for every join, the keys probed before any join is worth weighing), two of src/extract.ts (integerKeys taking any key an integer column leads, the cap ignored), two of src/write.ts (the confirmed line without the weak-evidence wording, fitForWriter returning Verified whole) and one of src/cli.ts (the summary counting every weighed join as weak evidence), each failing a named test in test/joins.test.ts, test/verify.test.ts, test/extract.test.ts, test/write.test.ts, test/scale.test.ts or test/integration.test.ts with its message. The summary sabotage first left every test green; the test of the keys now asserts the summary's line under each of its four settings, where one join is weighed and fits no other key, and the repeated sabotage failed it. Two sabotages first failed a test only with assert's own message; those comparisons were given messages saying what they check, and the repeated sabotages failed with them. Each file restored byte for byte each time." },
  { "task": "T3.3", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T3.3 iteration 2: ten sabotages of the task's core, eight of src/check.ts (the marker after the counts, the cap keeping the last rows, the table and the fold swapped, code() returning the name as it is, no pipe escaped in table(), code() keeping a name's line breaks, the reason a claim was not measured written into the After cell, diff() without its report format) and two of src/cli.ts (runCheck printing its report lines on stdout, a failed write of the comment passed over), each failing a named test in test/check.test.ts or test/remeasure.test.ts with its message. The cap sabotage first failed its test only with assert's own message; that comparison was given a message saying what it checks, and the repeated sabotage failed with it. No sabotage left every test green. The same iteration records four sabotages of its own fixes. Each file restored byte for byte each time." },
  { "task": "T4.1", "id": "A1", "part": "acceptance", "item": "The four automated scenarios pass in the Action repository's CI: the link to a green test.yml run on the main branch of FilipKalcic1/dbtruth-action, its scripts, scenarios and log jobs each green.", "evidence": "https://github.com/FilipKalcic1/dbtruth-action/actions/runs/36151534406 (main at 0cc5eb8): scripts, scenarios and log green. pass printed \"check fixture: 12 unchanged\", fail \"check fixture: 1 regression, 11 unchanged\", skipped the notice, error dbtruth's authentication sentence. The first run (36151307682) had scripts and scenarios green and log red, because gh api refuses to print a log with terminal escape sequences; fixed with curl in 0cc5eb8 (PROGRESS.md, T4.1)." },
  { "task": "T4.1", "id": "A2", "part": "acceptance", "item": "One comment per pull request, updated in place: the link to the test pull request in FilipKalcic1/dbtruth-action after two pushes, showing one comment, edited in place; the links to the two runs; and the scripts job's case that creates one comment in all, never two.", "evidence": "https://github.com/FilipKalcic1/dbtruth-action/pull/1, pushed twice (runs 36151722873 and 36151846504, all green): one comment by github-actions[bot] after the first push, still exactly one after the second. The second run found it by marker and author and sent a PATCH (no warning in its log, and no second comment although its report had findings, which would have created one); the body was identical, so GitHub records no edit. The scripts job's fake-gh cases show a changed body updated in place: one POST and two PATCHes in all. Closed without merging; branch deleted." },
  { "task": "T4.1", "id": "A3", "part": "acceptance", "item": "Forks are skipped without failing: the skipped scenario in the A1 run, an empty database-url as a pull request from a fork gets, with result skipped, the notice, and the step green.", "evidence": "https://github.com/FilipKalcic1/dbtruth-action/actions/runs/36151534406: the skipped scenario printed \"##[notice]dbtruth check skipped: database-url is empty, as it is on a pull request from a fork, which gets no secrets\", result skipped, the step green." },
  { "task": "T4.1", "id": "A4", "part": "acceptance", "item": "The database password never appears in logs or comments: in the A1 run, the log job, which fetched the scenarios job's real log and found the wrong password nowhere in it, and the error scenario, which found it in no file the Action wrote; and the test pull request's comment, which holds no URL.", "evidence": "https://github.com/FilipKalcic1/dbtruth-action/actions/runs/36151534406: the log job fetched the scenarios job's real log and found dbtruth's authentication line and not the password; the whole run log (1,144 lines) holds \"canary-pii-wrong\" 0 times; the error scenario found it in no file the Action wrote; the comment on pull request 1 holds no URL." },
  { "task": "T4.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored. For T4.1: the local sabotages of the Action's scripts and of scripts/make-fixture-snapshot.mjs, and a red test.yml run on a sabotage branch of FilipKalcic1/dbtruth-action (exit 2 reported as pass), the branch deleted afterwards.", "evidence": "PROGRESS.md, T4.1 iteration 3: eleven local sabotages, each made in place in the working copy and restored byte for byte from a copy kept outside both repositories. Seven of the Action's scripts, run through test/scripts.test.sh in node:22 with jq 1.6 and ShellCheck 0.9.0: in scripts/check.sh, exit 2 reported as pass (result=pass and exit 0 on a regression), the stop-commands dropped, the password echoed into the log (set -x) and into the could-not-run comment (the URL in its first line); in scripts/comment.sh, a second comment created where the first should be updated, the author filter dropped, the size cut dropped; each failed a named case with what it got and what it expected, and the Action's working tree was clean after each. Four of scripts/make-fixture-snapshot.mjs: the exit code mapping dropped, so the fixture's exit 2 passes through, and the run's directory taken from the process instead of <dir>, each failing \"the fixture snapshot script writes a context/ that check passes on\" (D4); a run that failed reported as exit 0, and the run's lines not passed to stderr, each failing \"the fixture snapshot script fails when the run does, and says why\" with its message. The exit 0 sabotage first left every test green, since D4 runs only a run that succeeds; that test was added, and the repeated sabotage failed it. The directory sabotage first failed D4 only with assert's own message; the comparison was given a message saying what it checks, and the repeated sabotage failed with it. git diff HEAD in dbtruth was as before after each restore. Iteration 2 records nine more sabotages of the Action's scripts, made in copies of its repository. Still to come from the lead: the red test.yml run on a sabotage branch of FilipKalcic1/dbtruth-action (exit 2 reported as pass), the branch deleted afterwards; its link belongs here. On GitHub: branch ci-sabotage (exit 2 reported as pass in scripts/check.sh), https://github.com/FilipKalcic1/dbtruth-action/actions/runs/36152049336, red: the scripts job failed four cases (\"a regression and two stale items fail: got ... result=pass ..., expected ... result=fail\") and the scenarios job failed; the branch was deleted, locally and on GitHub." },
  { "task": "T5.1", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T5.1 iteration 2: thirteen sabotages of the task's core, ten of src/mcp.ts (lookUp returning the relation before checking the columns named, the queue never advanced so calls ran at once, the per-call resetBudget dropped, a connection lost while idle kept, every join called inferred, the condition left out of the claim, JoinSchema a plain object that drops a key it does not take, the pairing refinement removed, describe_table answering the whole Table, context reading a directory listed under a table's name), two of src/safety.ts (a data exception told in the server's words, resetBudget keeping what was spent) and one of src/cli.ts (runMcp writing its settings lines to stdout), each failing a named test in test/mcp.test.ts or test/safety.test.ts with its message. Two first failed only with assert's own words or a bare key (the tool listing's comparison, describe_table's key checks); those checks were given messages saying what they check, and the repeated sabotages failed with them. Each file was restored byte for byte from a copy outside the repository, and git diff HEAD was as before." },
  { "task": "T5.2", "id": "A3", "part": "acceptance", "item": "Manual (BUILD_PLAN.md T5.2 A3): in Claude Code with the dbtruth MCP server added and the skill installed by init --skill, asking \"write a query joining orders to customers\" leads the agent to read context/ or call measure_join before it answers. Evidence: the Claude Code version, the date, the command that added the server, and the transcript excerpt with the context or measure_join call, or the Read of context/, before the answer.", "evidence": "An agent standing in for Claude Code, not Claude Code itself (no claude CLI on this machine): a fresh agent given only the skill, the fixture project that scripts/make-fixture-snapshot.mjs writes, and the four tools of the real dbtruth MCP server (started with --project on that project), asked 'write a query joining orders to customers'. Before answering it read context/README.md and context/tables/orders.md and customers.md, listed the tools, and called measure_join on orders.customer_id -> customers.id (broken, 440 of 500, 60 orphans all above the highest customers.id) and describe_table on both tables. It answered with a LEFT JOIN on purpose, said 60 of 500 orders (12%) point to no customer and how to keep or drop them on purpose, compared status with lower(btrim(status)), and left out api_token. PROGRESS.md, T5.2. A person should repeat it in Claude Code with the server added." },
  { "task": "T5.2", "id": "sabotage", "part": "tests", "item": "Sabotage check (BUILD_PLAN.md 4.5): what was broken, which test failed and with what message, and that it was restored.", "evidence": "PROGRESS.md, T5.2 iteration 2: sixteen sabotages of the task's core, each failing a named test in test/init.test.ts or test/skill.test.ts, or the package smoke test, with its message. Eleven of src/cli.ts (the skill installed in the working directory, the refusal dropped so an edited skill is written over, --force without its rm, --force removing a directory, the return before the skill when the .env could not be written, the skill read beside cli.ts, the skill written with the .env's text, --force reaching the .env, --skill not passed on by the command, rm without force: true, --force writing in place through a link), one of src/mcp.ts (measure_join renamed, the skill unchanged), one of package.json (skills dropped from files, failing the package smoke test with ENOENT on the installed skill) and three of skills/dbtruth/SKILL.md (the --reveal rule dropped, a line before the frontmatter, a name other than its folder's). Two left every test green (rm without force: true, and --force writing through a link); the tests \"init --skill --force installs the skill where there is none yet\" and \"init --skill --force puts a file of its own where the skill is a link, and leaves the linked file as it was\" were added, and the repeated sabotages failed them. Two failed only with assert's own words (the tool-name comparison, the name check); those were given messages, and the repeated sabotages failed with them. Each file was restored byte for byte from a copy outside the repository, and git diff HEAD was as before." }
]
